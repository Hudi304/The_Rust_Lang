

fn listening_to_a_TCP_connection(){
    
}

pub fn main() {
    // TCP is a lower level protocol
    // it descries how the information 
    // gets from on server to another
    // not specifying what the info is

    // HTTP is built on top of TCP 
    // defines the requests and responses
    // it's technically possible to use 
    // HTTP over other protocols 

    // but most of the time it's sent over TCP


}
